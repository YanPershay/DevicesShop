@model Device

<div class="col-lg-4">
    <img class="img-thumbnail" src="@Model.ImageUrl" alt="@Model.Name" />
    <h2>@Model.Name</h2>
    <p>@Model.ShortDesc</p>
    <p>Price: $@Model.Price</p>
    <p><a class="btn btn-warning" href="#">More</a></p>
</div>
