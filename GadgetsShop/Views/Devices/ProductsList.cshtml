
<h1>All devices</h1>
<h3>@Model.CurrentCategory</h3>
<div class="row mt-5 mb-2">
    @{
        foreach (Device device in Model.Devices)
        {
            @Html.Partial("AllDevices", device)
        }
    }
</div>
